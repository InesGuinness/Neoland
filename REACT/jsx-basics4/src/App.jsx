import "./App.css";
import Button from "./components/Button";

/**
 * Crea un botón que modifique un valor de false a true y renderice un contenido cuando este valor se modifique.
 */

// Define a functional component named App using arrow function syntax
const App = () => {
	// Return JSX representing the component's UI
	return (
		<>
			<Button />
		</>
	);
};

export default App;
